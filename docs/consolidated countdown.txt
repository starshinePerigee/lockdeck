non-asset todos:

BUGS
- superdrag still shows issues (superdrag to right, then left, stays on right)

GAME LOOP
- un-align
- update level generator
- new depths:
-- bind
-- reset
-- key
- death round logic

MENUS AND GAME FLOW
- top menu
- tutorial mode
- global settings object
- debug mode
- end of run screen
-- total loot
-- total picks
-- picks broken
-- favorite (most repaired + used)

MAIN OBJECTS
x end turn button
- current lock
- make deck / discard smaller?
- see deck contents
- see discard contents
- see broken pick status

META
- view deck (with trash forever)
- coins
- repair (show broken picks, trash forever, track tossed pick count)
-- track breaks
- basic shop
- basic deck management (show power, trash forever)
- pick modifiers (gems)
- pick effectors (jewelry)
- pick generator (bars)
- random shop picks
- basic lockpick pass and expansion

HINTS
x hint effect
x permanent safe
x multiple warn tracking

GUIDES
- card effect preview
- what happened last turn
-- track previous turn
-- icons for depth effects
-- offset right for each pins' iteration
-- boxes for hinted and activated
- card guide (hovering a pick card pops up a reminder box which shows tooltips for all depths on the card)
- depth description hover (hovering a depth pops up a tooltip on what it does)
- depth glossary that shows all depths that could be present in the current stage, as well as all card effects

ITCH
- 