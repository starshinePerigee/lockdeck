Locks should come in a lock-lock-boss sequence

which means locks should be effectively stand alone so you can go lock > lock directly

this means we need the following features:
1. the lock scene should pop the depth summary at the start
2. the lock should indicate "+1 coin time bonus" on completion

that means the meta flow is unchanged, except:
1. generate all three locks in advance, so you can display all depths in advance (maybe do some cool things with this, like all three locks use the same set)
2. temporary picks break "after this next burglary"
3. way more loot, but only at the end