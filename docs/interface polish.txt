Settings menu title is off-center and overlaps the border

CardDisplay should have the card grid full-panel and the title and close hint overlay (but the scroll position defaults to having enough space at the top for the title) (use a spacer?)

Make end turn visibly a button via outline

Move next depths into its own window, with tooltips and a scroll

RepairWidget is permanently fucked

Continue -> buttons change size on confirm, which is weird

Move strat hub's continue to the top? Redo strat hub entirely? Some kind of strategy hub theming? (A table with a map? A skyrim underdepths drawing?)